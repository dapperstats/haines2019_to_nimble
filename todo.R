tasks

 - flex the probabilities to match the mechanistic model

 - linear predictors algebraic setup?


currently working in the main script to embed each distribution in a model call
and verify the recovery of the density calc

m0 is in and does well!
perhaps a jump to m123 could happen? see if possible.