tasks

 - add poisson versions of each model
  
 - flex the probabilities to match the existing model
  - does not need to condition on detection!
  
 - look into parameter definition for flexible model structure
  - linear predictors
 
 - turn 123 into a non rectangular